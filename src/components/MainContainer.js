import dash from "../img/dash.png";
const MainContainer = () => {
  return (
    <div>
      <img className="h-screen" src={dash} alt="dashboard"></img>
    </div>
  );
};

export default MainContainer;
